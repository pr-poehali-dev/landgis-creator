# Структура компонентов проекта

Документация описывает архитектуру декомпозированных компонентов и принципы их организации.

## Принципы декомпозиции

### 1. Разделение ответственности
- **Презентационные компоненты** - только UI, получают данные через props
- **Логические хуки** - вся бизнес-логика, управление состоянием
- **Утилиты** - чистые функции без побочных эффектов
- **Композиционные компоненты** - соединяют логику и UI

### 2. Структура файлов
```
component/
├── ComponentName.tsx          # Главный компонент (композиция)
├── useComponentLogic.ts       # Бизнес-логика (хук)
├── ComponentPart1.tsx         # Подкомпонент 1
├── ComponentPart2.tsx         # Подкомпонент 2
└── utils.ts                   # Утилиты (если нужны)
```

## Декомпозированные компоненты

### 1. AdvancedFilterPanel

**Местоположение:** `src/components/AdvancedFilterPanel.tsx`

**Декомпозиция:**
```
src/components/
├── AdvancedFilterPanel.tsx           # Главный компонент (332 строки)
└── filter/
    ├── MapTypeSwitcher.tsx            # Переключатель Схема/Гибрид (42 строки)
    ├── FilterControls.tsx             # Кнопки Фильтры/Слои (53 строки)
    └── FilterPanelContent.tsx         # Содержимое панели фильтров (145 строк)
```

**Назначение компонентов:**

- **AdvancedFilterPanel.tsx** - главный компонент
  - Управляет состоянием фильтров
  - Загружает настройки с сервера/localStorage
  - Вычисляет доступные значения фильтров из properties
  - Композирует дочерние компоненты

- **MapTypeSwitcher.tsx** - переключатель типа карты
  - Кнопки "Схема" и "Гибрид"
  - Визуальная индикация активного режима
  - Адаптивность (скрывает текст на мобильных)

- **FilterControls.tsx** - кнопки управления
  - Кнопка "Фильтры" с бейджем количества активных фильтров
  - Кнопка "Слои" (опциональная)
  - Визуальная индикация открытой панели

- **FilterPanelContent.tsx** - содержимое панели
  - Активные фильтры с возможностью удаления
  - Кнопки "Сбросить всё" и "Закрыть"
  - Сетка колонок фильтров с опциями
  - Подсчет количества объектов для каждой опции

**Ключевые особенности:**
- Синхронизация настроек между устройствами через API
- Fallback на localStorage при отсутствии сервера
- Динамическая генерация колонок фильтров из данных
- Поддержка дефолтных значений фильтров

**Пример использования:**
```tsx
<AdvancedFilterPanel
  isOpen={isFilterPanelOpen}
  onToggle={() => setIsFilterPanelOpen(!isFilterPanelOpen)}
  filters={advancedFilters}
  onFiltersChange={setAdvancedFilters}
  properties={properties}
  mapType={mapType}
  onMapTypeChange={setMapType}
  onLayersClick={() => console.log('Layers')}
/>
```

---

### 2. DataTableDialog

**Местоположение:** `src/components/map/DataTableDialog.tsx`

**Декомпозиция:**
```
src/components/map/
├── DataTableDialog.tsx                # Главный компонент (109 строк)
└── datatable/
    ├── TableHeader.tsx                 # Шапка с фильтрами (117 строк)
    ├── TableBody.tsx                   # Тело таблицы (117 строк)
    └── useTableLogic.ts                # Хук с логикой (163 строки)
```

**Назначение компонентов:**

- **DataTableDialog.tsx** - главный компонент
  - Получает конфигурацию атрибутов через `useAttributeConfigs`
  - Генерирует заголовки таблицы
  - Композирует TableHeader и TableBody
  - Управляет состоянием Dialog

- **TableHeader.tsx** - шапка таблицы
  - Заголовок и счетчик объектов
  - Переключатель "С учётом фильтрации" / "Все участки"
  - Popover для скрытия/показа столбцов
  - Кнопка экспорта в Excel
  - Поле поиска

- **TableBody.tsx** - тело таблицы
  - Заголовки столбцов с сортировкой
  - Строки с данными объектов
  - Кнопка "На карте" для каждого объекта
  - Иконки сортировки (↑↓)
  - Плейсхолдер "Объекты не найдены"

- **useTableLogic.ts** - логический хук
  - Управление сортировкой (колонка, направление)
  - Управление видимостью столбцов
  - Поиск по всем полям
  - Фильтрация (с учетом фильтров / все объекты)
  - Экспорт в Excel через XLSX

**Ключевые функции:**

```typescript
// Сортировка
handleSort(headerKey: string)  // Переключает сортировку для колонки

// Видимость столбцов
toggleColumn(key: string)      // Показать/скрыть столбец

// Поиск и фильтрация
setSearchQuery(query: string)  // Поиск по всем полям
setShowFiltered(boolean)       // Переключить filtered/all

// Экспорт
handleExportToExcel()          // Скачать XLSX файл
```

**Пример использования:**
```tsx
<DataTableDialog
  open={isDataTableOpen}
  onOpenChange={setIsDataTableOpen}
  properties={filteredProperties}
  allProperties={properties}
  onShowOnMap={(property) => {
    setSelectedProperty(property);
    setIsDataTableOpen(false);
  }}
/>
```

---

### 3. AdminFilterSettings

**Местоположение:** `src/pages/AdminFilterSettings.tsx`

**Декомпозиция:**
```
src/pages/
├── AdminFilterSettings.tsx            # Главный компонент (111 строк)
└── admin-filter/
    ├── useFilterSettingsLogic.ts      # Логика (365 строк)
    ├── FilterSettingsHeader.tsx       # Шапка страницы (43 строки)
    └── filterLabels.ts                # Утилиты лейблов (21 строка)
```

**Назначение компонентов:**

- **AdminFilterSettings.tsx** - главная страница
  - Использует хук `useFilterSettingsLogic`
  - Рендерит AdminNavigation
  - Композирует FilterSettingsHeader, FilterColumnItem, FilterPreviewTable
  - Управляет диалогами редактирования и создания

- **useFilterSettingsLogic.ts** - логический хук
  - Загрузка настроек фильтров из properties
  - Drag-and-drop для изменения порядка столбцов
  - CRUD операции над столбцами фильтров
  - Синхронизация с сервером и localStorage
  - Управление диалогами редактирования

- **FilterSettingsHeader.tsx** - шапка страницы
  - Заголовок и описание
  - Кнопка "Сбросить" (возврат к дефолтным настройкам)
  - Кнопка "Создать столбец"
  - Кнопка "Сохранить" с индикацией загрузки

- **filterLabels.ts** - утилиты
  - `getStatusLabel()` - переводит статусы (available → Доступно)
  - `getTypeLabel()` - переводит типы (land → Земля)
  - `getOptionLabel()` - универсальная функция перевода

**Ключевые функции хука:**

```typescript
// Загрузка данных
loadFilterSettings()              // Загружает properties и извлекает атрибуты

// Drag-and-drop
handleDragStart(columnId)         // Начало перетаскивания
handleDragOver(e, targetId)       // Изменение порядка при перетаскивании
handleDragEnd()                   // Завершение перетаскивания

// Редактирование
toggleColumn(columnId)            // Включить/выключить столбец
openEditDialog(column)            // Открыть диалог редактирования
handleSaveEdit()                  // Сохранить изменения столбца
moveOption(index, direction)      // Изменить порядок опций
toggleDefaultValue(value)         // Переключить дефолтное значение

// Сохранение
handleSaveSettings()              // Сохранить в localStorage и на сервер
handleResetSettings()             // Сброс к дефолтным настройкам

// Создание/удаление
openCreateDialog()                // Открыть диалог создания
handleCreateColumn()              // Создать новый столбец
handleDeleteColumn(columnId)      // Удалить столбец
```

**Пример использования:**
```tsx
// В странице автоматически используется хук
const {
  filterColumns,
  handleSaveSettings,
  // ... остальные функции
} = useFilterSettingsLogic();

// Функции доступны для передачи в дочерние компоненты
<FilterSettingsHeader
  isSaving={isSaving}
  onSave={handleSaveSettings}
  // ...
/>
```

---

## Паттерны использования

### 1. Композиция vs Наследование
Используем композицию компонентов вместо наследования:

```tsx
// ✅ Хорошо - композиция
<Dialog>
  <DialogHeader>
    <TableHeader {...props} />
  </DialogHeader>
  <TableBody {...props} />
</Dialog>

// ❌ Плохо - наследование
class ExtendedDialog extends Dialog {
  // ...
}
```

### 2. Разделение логики и представления

```tsx
// ✅ Хорошо - логика в хуке
const MyComponent = () => {
  const {
    data,
    isLoading,
    handleSave
  } = useMyComponentLogic();
  
  return <UI data={data} onSave={handleSave} />;
};

// ❌ Плохо - всё в одном компоненте
const MyComponent = () => {
  const [data, setData] = useState([]);
  const [isLoading, setIsLoading] = useState(false);
  
  const handleSave = async () => {
    // 100 строк логики
  };
  
  return (
    // 200 строк JSX
  );
};
```

### 3. Props Drilling vs Context

Для передачи данных через 2-3 уровня используем props:

```tsx
// ✅ Хорошо для 2-3 уровней
<Parent>
  <Child onAction={handleAction} />
</Parent>

// ✅ Хорошо для глубокой вложенности
<ThemeContext.Provider>
  <DeepChild /> {/* использует useContext(ThemeContext) */}
</ThemeContext.Provider>
```

### 4. Мемоизация вычислений

Используем `useMemo` для тяжелых вычислений:

```tsx
// ✅ Хорошо - мемоизируем фильтрацию
const filteredItems = useMemo(() => {
  return items.filter(item => 
    item.name.toLowerCase().includes(search.toLowerCase())
  );
}, [items, search]);

// ❌ Плохо - пересчитываем каждый рендер
const filteredItems = items.filter(item => 
  item.name.toLowerCase().includes(search.toLowerCase())
);
```

---

## Рекомендации по рефакторингу

### Когда декомпозировать компонент?

1. **Размер файла > 300 строк**
2. **Множество обязанностей** (UI + логика + API + стили)
3. **Сложная вложенность** (более 3-4 уровней JSX)
4. **Дублирование кода** (похожие блоки в разных местах)
5. **Сложное тестирование** (невозможно протестировать изолированно)

### Шаги декомпозиции

1. **Определить границы ответственности**
   - Что делает компонент?
   - Какие независимые части можно выделить?

2. **Извлечь логику в хук**
   - Весь `useState`, `useEffect`, обработчики
   - Оставить только props и рендер

3. **Разделить UI на подкомпоненты**
   - Логические блоки (header, body, footer)
   - Повторяющиеся элементы (list items)

4. **Вынести утилиты**
   - Чистые функции (форматирование, валидация)
   - Константы и конфигурация

5. **Проверить работоспособность**
   - Билд проходит без ошибок
   - Функционал работает идентично
   - Тесты проходят (если есть)

### Пример пошаговой декомпозиции

**До (350 строк):**
```tsx
const BigComponent = () => {
  const [data, setData] = useState([]);
  const [filters, setFilters] = useState({});
  
  useEffect(() => { /* загрузка */ }, []);
  
  const handleFilter = () => { /* логика */ };
  const handleSort = () => { /* логика */ };
  
  return (
    <div>
      <header>
        {/* 100 строк */}
      </header>
      <main>
        {/* 150 строк */}
      </main>
      <footer>
        {/* 50 строк */}
      </footer>
    </div>
  );
};
```

**После декомпозиции:**

```tsx
// useBigComponentLogic.ts (100 строк)
export const useBigComponentLogic = () => {
  const [data, setData] = useState([]);
  const [filters, setFilters] = useState({});
  
  useEffect(() => { /* загрузка */ }, []);
  
  const handleFilter = () => { /* логика */ };
  const handleSort = () => { /* логика */ };
  
  return { data, filters, handleFilter, handleSort };
};

// ComponentHeader.tsx (50 строк)
const ComponentHeader = ({ filters, onFilter }) => {
  return <header>{/* UI */}</header>;
};

// ComponentBody.tsx (80 строк)
const ComponentBody = ({ data, onSort }) => {
  return <main>{/* UI */}</main>;
};

// ComponentFooter.tsx (30 строк)
const ComponentFooter = () => {
  return <footer>{/* UI */}</footer>;
};

// BigComponent.tsx (40 строк)
const BigComponent = () => {
  const { data, filters, handleFilter, handleSort } = useBigComponentLogic();
  
  return (
    <div>
      <ComponentHeader filters={filters} onFilter={handleFilter} />
      <ComponentBody data={data} onSort={handleSort} />
      <ComponentFooter />
    </div>
  );
};
```

---

## Преимущества декомпозиции

### 1. Читаемость
- Каждый файл решает одну задачу
- Легко найти нужную логику
- Понятная структура проекта

### 2. Тестируемость
- Можно тестировать хуки отдельно от UI
- Можно тестировать компоненты с моками
- Легче писать unit-тесты

### 3. Переиспользование
- Подкомпоненты можно использовать в других местах
- Хуки можно применять в разных компонентах
- Утилиты доступны везде

### 4. Поддержка
- Изменения локализованы в одном месте
- Меньше риск сломать другие части
- Проще делать code review

### 5. Производительность
- React.memo для подкомпонентов
- useMemo для вычислений
- Меньше ненужных ререндеров

---

## Антипаттерны

### ❌ Избыточная декомпозиция

```tsx
// Плохо - слишком мелкие компоненты
const ButtonIcon = () => <Icon name="Save" />;
const ButtonText = () => <span>Сохранить</span>;
const SaveButton = () => (
  <button>
    <ButtonIcon />
    <ButtonText />
  </button>
);

// Хорошо - разумный уровень
const SaveButton = () => (
  <button>
    <Icon name="Save" />
    <span>Сохранить</span>
  </button>
);
```

### ❌ Смешивание уровней абстракции

```tsx
// Плохо - бизнес-логика в UI компоненте
const TableRow = ({ data }) => {
  const processed = expensiveCalculation(data); // логика
  return <tr>{/* JSX */}</tr>;
};

// Хорошо - логика в хуке, UI отдельно
const TableRow = ({ data }) => {
  return <tr>{data.processed}</tr>; // только отображение
};
```

### ❌ Дублирование состояния

```tsx
// Плохо - дублируем props в state
const Child = ({ value }) => {
  const [localValue, setLocalValue] = useState(value);
  // localValue и value могут рассинхронизироваться
};

// Хорошо - используем props напрямую или controlled компонент
const Child = ({ value, onChange }) => {
  return <input value={value} onChange={onChange} />;
};
```

---

## Заключение

Декомпозиция компонентов — это баланс между простотой и гибкостью. Следуйте принципу: **"Начните с простого, декомпозируйте по необходимости"**.

### Контрольный список перед декомпозицией:
- [ ] Компонент > 300 строк?
- [ ] Сложно понять, что он делает?
- [ ] Есть дублирование кода?
- [ ] Сложно тестировать?
- [ ] Несколько независимых частей?

Если 3+ пункта "да" → декомпозируйте! 🚀

---

**Дата создания:** 2026-02-06  
**Версия:** 1.0  
**Проект:** LandGIS Creator
